# Prompt สำหรับ gen ข้อความดวงรายวันรอบใหม่

**เปลี่ยนวิธีใช้แล้ว** — ไม่ต้อง copy จากไฟล์นี้อีกต่อไป

ตอนนี้ไปที่หน้าแอดมิน → แท็บ **🔮 ดวงรายวัน** → กดปุ่ม **"คัดลอก Prompt เดือนถัดไป"** ได้เลย
ระบบจะดึงรายการข้อความที่มีอยู่แล้วทั้งหมด**สด ๆ จากฐานข้อมูล** (ไม่ใช่แค่ไฟล์ในเครื่อง) มาใส่ไว้ใน
prompt ให้อัตโนมัติ กันไม่ให้ AI เขียนซ้ำของเดิม — ไม่ต้องรันคำสั่ง Python เองอีกแล้ว

ขั้นตอน:
1. หน้าแอดมิน → 🔮 ดวงรายวัน → กด "คัดลอก Prompt เดือนถัดไป"
2. วางในแชต AI (Claude, ChatGPT ก็ได้) แล้วส่ง
3. เอา JSON ที่ตอบกลับมา (ต้องเป็น JSON ล้วน ๆ ไม่มี ```json ครอบ) เซฟเป็นไฟล์ หรือวาง/ลากเข้ากล่อง
   "วาง JSON ข้อความใหม่" ในหน้าเดียวกันได้เลย
4. กด "ตรวจ" แล้ว "เพิ่มเข้าคลัง"

เนื้อหา prompt ตัวจริงอยู่ที่ [`src/lib/fortunePrompt.ts`](src/lib/fortunePrompt.ts) — เก็บไว้ที่เดียว
เพื่อไม่ให้ไฟล์นี้กับปุ่มในแอดมินเพี้ยนไปคนละทางกัน
